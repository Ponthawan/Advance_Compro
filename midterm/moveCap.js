function capToFront(s) {
    var sp = s.split("");
    var caps = []
    var lower = []
    for (var i = 0; i < sp.lenght; i++)
        {
            if (sp[i] == sp[i].toUpperCase()){
                caps.push(sp[i]);
                
            }
        }
}